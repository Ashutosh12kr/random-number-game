Class: GuessingGame

Fields:

randomNumber: int – stores the random number to guess.

attempts: int – counts number of attempts.

scanner: Scanner – reads user input.

Constructor:

Initializes random number (1–100), sets attempts to 0, and creates a Scanner.

Method:

play() – runs the main game loop, accepts user input, gives feedback on guesses.
